/*
 Exercise 4: 
 This one is really just an extra before we continue the sadna

 Create a couple of users,
 Allow them to login only if they have the right duck!
 (Note that you can import the `birds` const from the previous exercise!)

 Tip:
 	Look inside the folders and files for clues and things that will help you!
*/

const users: unknown[] = [];

function login(user: unknown) {
  // ???
}

users.forEach((user) => {
  login(user);
});
